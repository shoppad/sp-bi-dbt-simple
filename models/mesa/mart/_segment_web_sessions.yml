# version: 2
# models:
#   - name: segment_web_sessions
#     description: >
#       A virtualized model combining all the Page Views records into a single Session.
#     meta:
#       metabase.caveats: The sessionization logic is not perfect, and may result in some sessions being split into multiple sessions.
#         The time range used to group Page records into Sessions can be adjusted in the DBT app.
#     columns:
#        - name: blended_user_id
#          description: This is the *best* user_id for the session. Could be a shop_subdomain or an anonymous ID.
