version: 2
models:
  - name: stg_shopify_partner_events
    columns:
      - name: shop_subdomain
        description: The foreign key for the Shop.
      - name: event_type
      - name: occurred_at
      - name: reason
      - name: description
