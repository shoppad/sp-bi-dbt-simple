version: 2
models:
  - name: calendar_dates
    description: A generated list of calendar days used to build timeline models.
    columns:
      - name: date_day
        description: The calendar day.
        data_tests:
          - unique
          - not_null
