version: 2
models:
  - name: stg_shop_entitlements
    columns:
      - name: shop_subdomain
        tests:
          - not_null
        description: The foreign key for the Shop.
      - name: name
      - name: value
      - name: status
        description: The usage status of the Shop.
