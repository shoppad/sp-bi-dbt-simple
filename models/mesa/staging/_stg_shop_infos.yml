version: 2
models:
  - name: stg_shop_infos
    columns:
      - name: shop_subdomain
        type: string
        length: 255
        primary: true
        data_tests:
          - not_null
          - unique
        description: The foreign key for the Shop.
