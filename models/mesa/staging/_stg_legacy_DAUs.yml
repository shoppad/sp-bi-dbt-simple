version: 2
models:
  - name: stg_legacy_DAUs
    description: Raw import of the legacy DAU tables (pre-Mesalytics)
    columns:
      - name: shop_subdomain
        description: The shop subdomain
        tests:
          - not_null
      - name: dt
        description: The date
        tests:
          - not_null
