version: 2
models:
  - name: stg_shopify_plan_changes
    columns:
      - name: shop_subdomain
        tests:
          - not_null
        description: The foreign key for the Shop.
      - name: plan
      - name: oldplan
      - name: plandisplayname
      - name: shopify_plan_change_id
        tests:
          - not_null
          # - unique
      - name: changed_at_pt
      - name: changed_on_pt
